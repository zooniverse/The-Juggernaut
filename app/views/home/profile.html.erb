<div id="page">
	<div id="header">
		<h1>Profile for <%= zooniverse_user %></h1>
	</div>
	
	<%= render :partial => "shared/flash", :locals => { :flash => flash } %>

	<div class="welcome">
		<p>You have made <%= pluralize(@recents.size, "classification") %> &middot; You have <%= pluralize(@favourites.size, "favourite") %></p>
	</div>
</div>
